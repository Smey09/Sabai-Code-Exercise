// 1. Write a declaration function that take a parameter called: “name”, and return a greeting message including that name

function greet(name) {
  return `Hi ${name} Welcome!`;
}

console.log(greet("JavaScrip"));
