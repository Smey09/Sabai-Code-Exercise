// 4. Write an anonymous function that calculates BMI (Body Mass Index). Formula: BMI = weight / (height * height)
const calculateBMI = function (weight, height) {
  return weight / (height * height);
};
console.log(calculateBMI(59, 1.75));
